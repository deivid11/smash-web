/** Build-time constants inlined by the bundler (`gameDefine` in vite.config.ts).
 * Read them through web/src/version.ts, which falls back when they are absent. */
declare const __GAME_VERSION__: string | undefined;
declare const __GAME_BUILT__: string | undefined;
