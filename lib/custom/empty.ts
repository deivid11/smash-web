import type { InstalledPack } from './types.ts';
/** Node tooling / installations with no opted-in packs. Vite supplies the selected local packs. */
const packs: readonly InstalledPack[] = [];
export default packs;
